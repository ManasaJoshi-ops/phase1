package finalkey;

public final class FinalKeyword {
	
     public static void main(String[] args) {
	
    	 //final in inheritance---->> cannot be inherited(if you mark a class as final it cannot be implemented
    	 //so abstract class can never be final (as it has be inherited)
    	 //final in variables---->> cannot be changed
    	 final int x=8;
    	 // x=0; (cannot be done)
    	 
    	 //for methods --->> it cannot be overridden
}
}
